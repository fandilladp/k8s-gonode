apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgree-db.fullname" . }}-test-connection"
  labels:
    {{- include "postgree-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgree-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
